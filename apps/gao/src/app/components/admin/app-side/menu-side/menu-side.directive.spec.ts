import { MenuSideDirective } from './menu-side.directive';

describe('MenuSideDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuSideDirective();
    expect(directive).toBeTruthy();
  });
});
